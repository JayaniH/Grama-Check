# +required Version of the endpoint configuration YAML
version: 0.1

# +required List of endpoints to create
endpoints:
  - name: police check API
    port: 9090
    type: REST
    networkVisibility: Organization
    context: /policeCheck
    schemaFilePath: policeCheck_openapi.yaml